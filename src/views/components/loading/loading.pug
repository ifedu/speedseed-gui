div('*ngIf'='loading.on')
    .background.omg.size
    .spinner.omg.size.flex-center-content
        mat-progress-spinner(
            color='primary'
            mode='indeterminate'
            value='50'
        )
