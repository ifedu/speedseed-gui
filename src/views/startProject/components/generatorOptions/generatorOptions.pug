mat-grid-list(
    '*ngIf'='data.generator.options.template'
    cols='4'
    rowHeight='60px'
)
    mat-grid-tile
        mat-form-field
            input(
                '[(ngModel)]'='data.generator.options.project'
                matInput
                placeholder='project'
            )

    mat-grid-tile('*ngFor'='let generatorOption of data.generatorOptions')
        mat-form-field
            mat-select(
                '[(ngModel)]'='data.generator.options[generatorOption.name]'
                '[placeholder]'='generatorOption.name'
            )
                mat-option(
                    '*ngFor'='let choice of generatorOption.choices'
                    '[ngClass]'='{"none": checkGeneratorChoice(generatorOption, choice)}'
                    '[value]'='choice.value'
                ) {{choice.value}}
